package iplist

// These docs-derived name tables are optionally populated by the generated file
// docs_names_gen.go.
//
// We keep them as vars (initialized by init()) so the package still compiles
// in a minimal checkout where go generate hasn't been run.
var (
	docsCountryKeys string
	docsCountryVals string
	docsCountryOff  []uint32

	docsCNCityKeys string
	docsCNCityVals string
	docsCNCityOff  []uint32
)
